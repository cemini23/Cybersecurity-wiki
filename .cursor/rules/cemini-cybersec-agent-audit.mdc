---
description: Agent runtime + skill audit stack — ClawAudit, SkillSpector, DefenseClaw, SkillSec, ecc-agentshield (Cybersec Phase-1)
alwaysApply: true
---

# Agent runtime / skill audit (Cybersec Phase-1)

Pre-install and local-runtime checks. Scanner disagreement is expected — no single allow/block oracle.

## Before enabling a skill / MCP / agent config

- Run federation `cursor-security-preflight` (or `--local`) after skill sync / before third-party Cursor skill or MCP enable.
- Prefer static SkillSpector-style `--no-llm` preingest when available; do not treat SaaS Skillgate alone as a production allowlist.
- Layer DefenseClaw **observe** (or equivalent) before any action/enforcement mode; optional LLM judge only with explicit operator key.

## Local runtime audit

- Complement catalog scanners with **source/runtime** audit methodology (ClawAudit Semgrep/CodeQL patterns) on agent harness code you own or fork.
- Do not clone or redistribute ClawAudit until LICENSE is verified; methodology steal-from is fine.
- SkillSec lifecycle: treat skills as five-stage (author → pack → install → invoke → evolve) — re-audit on evolution/self-modify paths.
- **K269 Taboo (audit)** — decoding-time taboo diagnostic (2608.09900) as a **pre-deployment refusal-surface audit**: mask top refusal tokens on an adversarial prompt; immediate policy-violating output ⇒ shallow top-token alignment, not latent safety. Keep as a scoped eval; do not use as a generic safety benchmark without negative controls (HumanEval, MCQ floor). REFERENCE (Zenodo CC-BY-4.0).
- **K271 REDAgentBench (audit)** — when auditing agent-safety claims, require **faithful ASR reporting**: separate exposure→execution→observation→adjudication and state-grounded verification (service receipts / final-state diffs) rather than trajectory self-report; never accept a single harness-agnostic ASR as an agent-safety certificate. REFERENCE (benchmark not yet released).
- **K276 withhold contract (audit/harness)** — when an agent must **refuse a capability it has** (refusal-under-knowledge), enforce withholding as a **per-turn machine-checkable contract**, not a prompt: binding decision in a **non-LLM policy core fed only trusted state** (injection-proof, unit-testable), a **deterministic detector** for the forbidden output that outranks model judgment, and a **collusion-resistant judge** on risky turns (never sees the raw untrusted input). Calibrate with a **reason-capturing loop** (scripted adversarial personas → stronger auditor; record each rejection's reason — violations descend an ordered gross→subtle "over-help ladder"). Treat **over-blocking as a measured failure**, prefer revise over refuse; **HITL before mutating any prod harness**. REFERENCE (deployed tutor, no public code).

## Defaults

- Default **deny** self-modifying / self-evolving agents on write-capable MCP surfaces.
- `ecc-agentshield` npm is CONDITIONAL — install only when operator asks; full ECC marketplace bundle stays Steal-from.
- Typosquat / star-inflated security CLIs: verify package name + LICENSE before `npm i` / PATH install.

- **K278 ATOBench (audit)** — when auditing pentest/agent-verification claims, do not treat high activity or a plausible report as evidence of grounded verification. Audit the **verification chain**: was the final claim supported by source-level evidence (not just trajectory self-report)? Pairs with faithful-ASR (K271) — ATOBench is the offensive-pentest-specific instance. REFERENCE (no code).
- **K283 JailbreakSkill (audit)** — do not promote/refine `.cursor/skills` from an attack-skill evolution run. HITL on a skill write does not cover retrieval-time harm (pairs misevolution). Pattern steal only (null SPDX).
- **K290 CHIVE (audit)** — explanations of model/agent behavior (CoT, SAE write-ups, review narratives) are not evidence unless **counterfactual-tested**. Interpretability tools showed no uplift on in-the-wild behaviors. GO REFERENCE clone `.local/adopts/chive` (MIT); runtime **wont_wire**.
- **K240 Tripwire (audit)** — do not neuron-clamp abliterated lab models without HITL. Watch only; no jailbreak PoC; no Qwen 27B abliterated / Uncensored FP8 weight download. Dual-ID: ≠ OSINT/GW Talon K240, ≠ CCC robotics K240.
- **CCC K290 excess-authority (audit)** — score **trajectories** for authority beyond the task envelope (six-dimensional deterministic pre/post audit), not only final-answer correctness. Learned restraint complements gates / Mandato signed mandates / sandboxing — it does not replace them. **Dual-ID: CCC K290 (2608.18351) ≠ Cybersec K290 CHIVE (2608.16747).** No public code; authorized lab eval only.
- **Rule-blindness 2608.16852 (audit)** — a compliance-guard verdict is not rule compliance until a **crossed-rule** counterfactual (two rules × two scenarios) shows the verdict tracks the stated clause, not scenario features. ICS is an inexpensive audit scalpel, not a production detector. Watch; no FujitsuResearch clone as this paper's artifact; no jailbreak PoC.
- **K297 TI→detection (audit)** — LLM-emitted Sigma is not deployable without template grounding + separate validity/relevancy/ATT&CK-coverage scores + human SIEM review. REFERENCE.
- **K298 Inadvertent Context Leakage (audit)** — refusal ≠ no leak. Do not put secrets in model context if outputs are third-party-visible (email, Slack, PR bodies, "write a paragraph with numbers"); prefer **tool-layer grants that never return the secret to the planner** (1Password pattern; this wiki: `scripts/secret_grant.py`). Add *benign-output* predicate tests to executable eval, not only jailbreaks. Cursor deny hook blocks `.env` reads (`scripts/k303_k298_policy.py`).
- **K299 TrustRAG committee RAG (audit)** — RAG ranking/provenance is an **integrity boundary**; schema-valid retrieved chunks are not authenticated without committee/hash replay (pairs ESTI / ADI). **Name collision: this is NOT Zhou/HuichiZhou TrustRAG (2501.00879) nor gomate-community — no clone** (no public SPDX).
- **K300 BreakGuard dependency tests (audit)** — LLM-generated dependency tests are **candidates**; crash-type > behavioral; human review before treating as an update gate. **Name collision: ≠ ProgrammerNomad BreakGuard (Windows break-reminder app); no matching repo found at hunt → REFERENCE.**
- **K301 CLEAR (audit)** — a global safety LoRA/SFT is **not** a proof of safety: it trades utility for refusal (alignment tax) and says nothing about conditional/selective behavior. If a stack must keep benign behavior intact, prefer conditional/selective intervention and evaluate over-refusal (XSTest-style), not just ASR. No weight downloads.
- **K302 PsychJail (audit)** — interactive LLM deployments are social-engineering surfaces: **refusal is not turn-stable** across multi-turn persuasion. Multi-turn persuasion eval belongs in the **authorized lab** only; no persuasion recipes in wiki. Guardrail verdicts should be validated across turns, not just per-message.
- **K303 NL rules ≠ deny (audit)** — CLAUDE.md / rule prose is a **write-only channel**, not enforcement (only ~4–16% of real security rules match a built-in control; strict 4.4%, CI 2.6–6.7%; extraction recall 66.3%). Do not treat instruction-file prose as a control; prefer built-in deny / permission rules / sandbox / PreToolUse hooks. Secrets never in NL rules only. **This wiki runtime:** `.cursor/hooks.json` + `scripts/k303_k298_policy.py` (fail-closed) deny `.env` / key files; `python3 scripts/secret_grant.py -- <cmd>` is the grant path. Dual-ID overlay survives CCC overwrite via `scripts/restore_cybersec_dual_id.py`.
- **K304 RIM (audit)** — reasoning FT is a **safety event**: fine-tuning on harmless reasoning data can weaken safety (conditional — Qwen2.5-3B/7B reproduce it). Re-run safety evals after any fine-tune of an aligned model; track safety-representation drift (R/S directions, safety-decision layers). No clamp recipes.
- **K306 LLM compliance artifacts (audit)** — LLM-generated compliance artifacts (DPIA/DPP) are **candidates, not deliverables**: strict-format outputs are consistent but hallucinate more; vague standards need more context. HITL required; schema-valid ≠ correct; private data stays off third-party APIs.
- **K307 StepGuard (audit)** — prefer **pre-execution step-level** guards for MCP/tool agents over trajectory-only audit; report ASR **and** benign utility together; defense bias (over-block vs under-block) is a measured failure. `zheng977/StepGuard` had no LICENSE at hunt — re-hunt via `scripts/stepguard_inventory.sh check` (still **HOLD** 2026-08-28); adopt only after SPDX. REFERENCE only; **wont_wire** default MCP. Dual-ID: ≠ CCC MediSkill-Evo K307.
- **K308 decorative CoT (audit)** — visible chain-of-thought is **not evidence** unless counterfactual-tested (cdr / perturbation audit); high answer accuracy + plausible CoT can still decouple (~73% panel cdr on destructive edits in medical QA). Pairs K290 CHIVE. Dual-ID: ≠ CCC MetaCaster K308.
- **K309 prompt security redistribution (audit)** — structured/security codegen prompts improve **compliance** (fewer refusals) but often **redistribute** Bandit/CodeQL findings rather than reduce prevalence; dominant CWE classes persist. Do not treat prompt hardening as SAST substitute; watch semantic drift. Dual-ID: ≠ CCC Prime Agent K309.
- **K310 RTLGuard (audit)** — third-party / open-weight fine-tunes are latent trust hazards: a poisoned RTL/codegen model can emit a **hardware Trojan** on a benign prompt. **Sanitize-before-trust**: run a clean-teacher distillation/feature-alignment pass against a trusted reference before trusting a codegen model's output; provenance alone does **not** clear a backdoor. No public repo at hunt → REFERENCE; no clone; no trojan payloads. Pairs cweep (design-time CWE lint) + Gradient Immunity / DataShield (release-time gates). Dual-ID: ≠ CCC K310 AP2.
- **K311 CTF-ABACUS (audit)** — a recovered flag is **not** demonstrated exploitation (trace-verified exploits are only **62–87%** of recovered flags across 1,435 attempts / 240 challenges / six models / two judge lenses). Audit the trace: exploitation phase + technique, where the flag first appears, whether it is backed by demonstrated behavior. Report a **trace-verified rate**, not a flag count. Pairs K271 faithful ASR + K278 ATOBench. Authorized-lab eval only. Dual-ID: ≠ CCC K311 SCOUT.
- **K312 loop composition (audit)** — per-trajectory / per-turn guards do **not** compose across unattended loops: fragmented evidence gives a trajectory-scoped monitor **TPR = FPR**. Audit a **loop-level, non-decaying safety state** that survives trajectory boundaries (pairs K307 step-level guard, which is the enforcement *point*, not the composition fix). **Executable:** `scripts/k312_loop_state.py` + `.cursor/hooks.json`; `python3 scripts/k312_loop_state.py status`. Dual-ID: ≠ CCC K312 StepGuard (Cybersec StepGuard = K307).
- **K313 RedEvoAgent (audit)** — evolving an attack skill from red-team trajectories is a **lab eval primitive**, not a prod capability: gate every update on a **validation ratchet** (keep only updates that improve validation) and **attribute credit to the tool**, not the trajectory. Never auto-evolve `.cursor/skills` from an attack run; no attack-skill bodies/PoCs in wiki. Pairs K283 JailbreakSkill + skill-misevolution. Dual-ID: ≠ CCC K313 StarHarness.
- **K314 Recognition–enforcement gap (audit)** — verbalized or linearly decodable instruction-source recognition is **not** tool execution enforcement under permissive configs (GPT-4.1-mini: ~99% verbal detection vs ~99% execution). Do not treat model self-arbitration ("follow the most authoritative instruction") as a security boundary; prefer **external reference monitor** (authenticated source routing + capability-gated tools). Report fleet metrics with **model-clustered bootstrap CIs** — naïve trial-level CIs underestimate variance ~4.5×. Residual boundary is **semantic authorization** through authenticated channels. InstructionArbitrationBench = authorized-lab eval when released; **no spoof templates in wiki**. Pairs K303 NL≠deny + K276 withhold contract + K277 measurement integrity + K307 StepGuard. Dual-ID: ≠ CCC K314 Recuris.
- **K315 Security-agent SLR (audit)** — when auditing security-agent papers/benchmarks, classify on **Approach / Application / Assessment** axes; demand **bounded authority + auditable trajectories**, not terminal success alone. Final task success does not prove faithful evidence use, scope adherence, permission respect, or cost. Pairs K271 faithful ASR + K311 trace-verified CTF eval. Survey REFERENCE only — no clone. Dual-ID: ≠ CCC K315 ToolMinimize.
- **K316 SIR CUA IPI (audit)** — score computer-use IPI with **deterministic VM/state oracles** and **joint success** (adversarial + benign task), not LLM judges or text-only ASR. Adaptive principle libraries are a **lab eval primitive** — no injection/principle payloads in wiki. HF space `TrustSafeAI/SIR` WATCH. Dual-ID: ≠ CCC K316 LifePlanner.
- **K317 EvoSkill Injection (audit)** — autonomous **skill generation/evolution** is an attack surface: measure **stored → retrieved → activated** persistence, not single-turn harm. Never auto-evolve `.cursor/skills` from adversarial trajectories; HITL on skill write does not cover retrieval-time harm. No public SPDX at hunt. Dual-ID: ≠ CCC K317 TAU-Agent.
- **K318 J-lens (audit)** — multi-token SAE verbalizations are **hypotheses** for refusal-surface mapping; counterfactual-test before hardening (pairs K290 CHIVE). **Not enforcement** — pairs K314 recognition≠enforcement. Neuronpedia WATCH. Dual-ID: ≠ CCC K318 ProgRouter.
- **K320 EvoFlint (audit)** — multi-turn red-team ASR must name **harness, judge, split, and severity metric**; archive/category coverage is part of the claim, not a scalar ASR alone. HF EvoFlint WATCH; no plan payloads in wiki. **Runtime:** `scripts/k320_evoflint_redteam_precheck.py` (advisory checklist). Dual-ID: ≠ CCC K320 Claude Code handbook.
- **K321 Guardrail construct validity (audit)** — before licensing guardrail welfare/safety lifts in agent simulations, run **incentive validity + protocol isolation + stochastic stability + welfare accounting**; return Invalid/Inconclusive when schema/chooser drift. Pairs K277 measurement integrity. **Runtime:** `scripts/k321_guardrail_construct_validity_precheck.py` (advisory checklist). Dual-ID: ≠ CCC K321 ASIL structured state.
- **K322 Firmware rehost (audit)** — document rehost fidelity acceptance criteria on owned hardware; reactive peripheral modeling; emulator ≠ device. **Runtime:** `scripts/k322_firmware_rehost_precheck.py` (advisory checklist). Dual-ID: ≠ CCC K322 MoRe.
- **K323 CodePoisonRAG (audit)** — upstream RACG poisoning is a **retrieval trust boundary**; measure Top-k retrieval + ASR with stated corpus ratio; context-only defenses are partial. **No poison templates in wiki.** Dual-ID: ≠ CCC K323 intent-as-a-tool. **Runtime:** `scripts/k323_codepoisonrag_precheck.py` (advisory checklist).
- **K324 SafeEvolve (audit)** — harness-policy co-evolution must stay **bounded, reversible, HITL-gated**; never unattended prod skill/harness auto-evolve (pairs K237 misevolution). Dual-ID: ≠ CCC K324 RedEvoAgent. **Runtime:** `scripts/k324_safeevolve_precheck.py` (advisory checklist).
- **K325 Linguistic illegibility (audit)** — CoT / NL self-report / linguistically-defined probes are **not a complete security boundary**; require external sandbox floor (taint tracking, isolation). Pairs K308 decorative CoT + K314 recognition≠enforcement. Dual-ID: ≠ CCC K325 security-agent SLR steal.
